# AXI3-UVM-VIP
A UVM-based Verification IP for the AMBA AXI3 protocol, featuring master &amp; slave agents, scoreboard, monitor, protocol assertions, and functional coverage. Supports burst types (INCR, FIXED, WRAP). Includes automated compile/run flow using Perl scripts.
